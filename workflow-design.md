# 套餐购买工作流程设计

## 1. 总体流程

```
用户选择API → 查看可用套餐 → 选择套餐 → 创建订单 → 支付订单 → 获得API使用权限
```

## 2. 关键步骤

### 2.1 套餐展示与选择
- 用户在创建订单页面选择API
- 系统自动加载该API的所有可用套餐
- 套餐卡片展示名称、价格、调用限制、并发限制和有效期
- 用户选择合适的套餐后点击"确认订单"

### 2.2 订单创建
- 系统根据用户选择的API和套餐创建订单
- 订单状态初始为"待支付"(pending)
- 订单记录套餐的所有关键属性(调用限制、并发限制、有效期)
- 创建成功后跳转到订单管理页面

### 2.3 订单支付
- 用户在订单管理页面查看待支付订单
- 点击"支付"按钮，选择支付方式
- 输入交易ID完成支付
- 系统更新订单状态为"已支付"(paid)

### 2.4 额度更新
- 订单支付成功后，系统自动更新用户的API调用额度
- 增加相应的API调用次数
- 设置API调用有效期
- 更新并发调用限制

## 3. 数据流转

```
ApiPlan → Order → UserQuota
```

- ApiPlan提供套餐基础数据
- Order记录用户购买的套餐信息
- UserQuota跟踪用户的API使用额度和权限

## 4. 用户界面

### 4.1 创建订单页面
- API选择下拉框
- 套餐列表(卡片式展示)
- 确认订单按钮

### 4.2 订单管理页面
- 订单列表(表格式展示)
- 订单状态标签(待支付、已支付、已取消)
- 操作按钮(查看详情、支付、取消)
- 订单详情弹窗
- 支付弹窗

## 5. 安全与验证

- 验证用户登录状态
- 验证API和套餐的有效性
- 防止重复支付
- 确保订单数据与套餐数据一致性
